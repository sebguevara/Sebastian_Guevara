/** Shared motion tokens — soft, barely-there entrances. */
export const motion = {
  ease: "power1.out",
  duration: 1.35,
  heroDuration: 1.25,
  heroY: 8,
  heroStagger: 0.14,
  revealY: 8,
  revealStart: "top 85%",
} as const;
