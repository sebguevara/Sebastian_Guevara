---
interface Props {
  id: string;
  class?: string;
}

const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class:list={["section w-full scroll-mt-20 lg:scroll-mt-8", className]}
>
  <slot />
</section>
