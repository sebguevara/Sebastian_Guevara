---
interface Props {
  eyebrow: string;
  title: string;
}

const { eyebrow, title } = Astro.props;
---

<header class="mb-10 sm:mb-14" data-animate="reveal">
  <p class="eyebrow mb-3">{eyebrow}</p>
  <h2 class="section-title">{title}</h2>
</header>
